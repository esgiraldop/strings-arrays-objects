import {calcAvg, askMarks} from "./1_avgMarksCalculator.js"

const main = () => {
    let msg = "Please enter your marks separated by commas. Only numbers from 1 to 10 allowed: "
    let marks = askMarks(msg)
}